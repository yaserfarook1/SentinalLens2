# ========== FRONTEND CONFIGURATION EXAMPLE ==========
# IMPORTANT: This file is a TEMPLATE ONLY
# NEVER commit the actual .env.local file to Git
# NEVER put secrets in this file
#
# For local development:
# 1. Copy this file to .env.local
# 2. Fill in the values from your Azure app registration
# 3. Never commit .env.local
#
# PUBLIC values only (prefixed with NEXT_PUBLIC_ are exposed in browser)

# ========== API CONFIGURATION ==========
# Backend API URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ========== AZURE ENTRA ID (App Registration) ==========
# Create app registration: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps/
# Get values from: Overview tab

# Application (client) ID
NEXT_PUBLIC_CLIENT_ID=<your-app-registration-id>

# Directory (tenant) ID
NEXT_PUBLIC_TENANT_ID=<your-tenant-id>

# Optional: Redirect URI after auth (defaults to http://localhost:3000/auth/redirect)
# NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000/auth/redirect

# ========== FEATURE FLAGS ==========
# NEXT_PUBLIC_ENABLE_DEBUG_PANEL=false
